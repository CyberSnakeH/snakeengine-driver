# SnakeEngine driver character device.
#
# The installer/package creates the snakeengine system group before this rule is
# loaded. Users that should access /dev/snakedrv must be members of that group.

ACTION=="add|change", KERNEL=="snakedrv", SUBSYSTEM=="snakedrv", MODE="0660", GROUP="snakeengine", SYMLINK+="snake"
